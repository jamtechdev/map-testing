export const sWidth = window.innerWidth
export const sHeight = window.innerHeight
export const osdWidth = window.innerWidth
//If I use orignal window.innerWidth then the svg layer will move bottom to the OSD image.
export const osdHeight = window.innerWidth/2
export const labelWordCount = (word) => {
    return word.split(" ").length;
  };
  //Description box style
  export const descriptionBoxStyle = {
    display: "block",
  };

  